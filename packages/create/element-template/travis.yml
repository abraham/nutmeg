os: linux
dist: bionic
language: node_js
node_js:
  - node
before_script:
  - node --version
  - npm --version
  - npm install
script:
  - npm test
cache:
  directories:
  - node_modules
addons:
  firefox: latest
  chrome: stable
