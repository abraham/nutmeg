environment:
  matrix:
    - nodejs_version: "9"
    - nodejs_version: "8"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install --no-optional
  - npm install --global . --no-optional

test_script:
  - node --version
  - npm --version
  - npm test

build: off
