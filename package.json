{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --no-ci",
    "test": "lerna run test",
    "prerelease": "lerna version --exact",
    "release": "lerna publish from-git"
  },
  "devDependencies": {
    "lerna": "3.4.3",
    "rimraf": "2.6.2"
  }
}
